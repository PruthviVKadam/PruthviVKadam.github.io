# assets/

Drop here:
- `Pruthvi_Kadam_Resume.pdf` — linked from the header "Résumé" button in `index.html`.
- Project demo GIFs (≤3 MB each) — embed in the matching project card later.

Keep each file small so the Pages site stays fast (Lighthouse > 90).
